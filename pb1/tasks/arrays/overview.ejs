<%
    var variant_id = parseInt($var);
	var task1Vars = [
		', які менші `0`',
		', які більші `50`',
		' у межах від `[-10..10]`',
		' у межах від `[-50..50]`',
		' у межах від `[-99..127]`',
		', які більші `100`',
		', які менші `50`',
		', які більші `127`',
		', які менші `-25`',
		', які більші `-25`',
		', які менші `-25` або більші `25`',
		', які менші `-50` або більші `100`',
		', які менші `-50` або більші `150`',
	];
	
	var task2Vars = [
		'першого мінімального',
		'першого максимального',
		'першого найменшого додатнього',
		'першого найбільшого від\'ємного',
		'першого мінімального в межах від `[-50..50]`',
		'першого максимального в межах від `[-50..50]`',
	];
	
	var task1 = task1Vars[variant_id % task1Vars.length];
	var task2 = task1Vars[(variant_id + 2) % task1Vars.length];
	var task3 = task1Vars[(variant_id + 5) % task1Vars.length];
	var task4 = task2Vars[(variant_id) % task2Vars.length];
	var task5 = task2Vars[(variant_id + 3) % task2Vars.length];
%>

### One-Dimensional Arrays

__Завдання__:

1. Згенерувати масив `A` випадкових цілих чисел (у інтервалі `[-99..199]`) розміром `10` елементів і вивести його у консоль.
1. Вивести елементи масиву `A` і виділити певним кольором фону всі числа<%= task1 %>.
1. Вивести елементи масиву `A`, виділити певним кольором фону всі числа<%= task2 %> і у окремих рядках вивести їх кількість, суму та середнє дробове значення.
1. Знайти і вивести у окремому рядку значення та індекс <%= task4 %> числа масиву `A`.
1. Знайти і вивести у окремому рядку значення та індекс <%= task5 %> числа масиву `A`.
1. Створити окремий масив символів `S` і записати у нього всі числа із масиву `A` за правилом: взяти модулі відповідних елементів масиву `A`, поділити по модулю на `95` і додати `32`. Замінити останній елемент масиву `S` на `\0` і вивести масив `S` за допомогою функції `puts()`.
1. Обнулити у масиві `A` всі числа<%= task3 %> та вивести його, виділивши певним кольором фону відповідні нульові елементи масиву.

Перед виводом результату кожного завдання вивести у окремому рядку консолі строку, що описує відповідне завдання. 

Вивід масиву робити у окремому рядку, елементи масиву розділяти пробілами або символами горизонтальної табуляції.

Наприклад:
~~~~
3. Numbers greater than '0':
-10	[88]	0	-10	-236	[12]	-45	[70]	-89	[100]
Count: 		4
Sum: 		270
Average:	67.500000
~~~~

Компілювати програму за допомогою команди:
~~~~
gcc main.c -std=c89 -Wall -Werror -pedantic-errors -lm -lprogbase
~~~~

Матеріали:

1. [Лекція 3. Масиви](https://docs.google.com/document/d/180QGY8Ffg22r0VIGwmkB9_XSsf3rLLvLG3YsEh8a2qQ/edit#)