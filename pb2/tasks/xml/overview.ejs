<%

  var tasks = [
  
`Пацієнт клініки`,
`Вчитель у школі`,
`Співробітник на підприємстві`,
`Директор стартапу`,
`Водій таксі`,
`Учень`,
`Пенсіонер`,
`Фрілансер `,
`Водій маршрутки`,
`Розробник програмного забезпечення `,
`ScrumMaster `,
`Адміністратор групи у соціальній мережі`,
`Працівник закладу швидкого харчування (касир)`,
`Директор підприємства`,
`Інвестор`,
`Викладач у вищому навчальному закладі `,
`Науковець`,
`Дизайнер`,
`Музикант`,
`Кінорежисер`,
`Ведучий на радіо`,
`Кіноактор`,
`Розробник у Scrum Команді`,
  ];

  var task = tasks[parseInt($var) % tasks.length];
%>

### XML Data Format

Створити новий тип даних для сутності '<%= task %>'. 

Цей тип даних, окрім своїх властивостей (мінімум 3 поля, строкове, цілочисельне і дробове), повинен містити масив інших сутностей.
Наприклад, сутність Студент містить масив Академічних Предметів (кожен елемент - строка назви предмету).

Реалізувати список для зберігання елементів типу заданої сутності.

Створити модуль __XmlLoader__ із функціями (замість TEntity підставте назву типу заданої сутності):

```
void XmlLoader_saveToString(char * str, const TEntity * entity);  // 1
void XmlLoader_loadFromString(TEntity * entity, const char * xmlString);  // 2
void XmlLoader_saveListToString(char * str, const TEntityList * entityList);  // 3
void XmlLoader_loadListFromString(TEntityList * entityList, const char * xmlString);  // 4
```

Функція 1 через вказівник на сутність зберігає її вміст у строку у форматі XML (при цьому потрібно використати хоча б один XML-атрибут).

Функція 2 зі строки із XML вмістом заповнює поля сутності через вказівник на неї.

Функція 3 аналогічна функції 1, але зберігає не одну, а список сутностей.

Функція 4 аналогічна функції 2, але вона створює і заповнює нові елементи списку сутностей.

Описати модульні тести для перевірки функцій 1 та 2.

