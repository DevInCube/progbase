<%
    var tasks = [
       [`__@todo__`, `__@todo__`, `__@todo__`]
    ];
    var task = tasks[parseInt($var) % tasks.length];
%>

### TCP Client

Реалізувати консольну програму, за допомогою фреймворка __Qt__, 
що виконує запит до сервера `progbase.herokuapp.com` і по шляху `<%= task[1] %>` отримує у відповідь 
строку у форматі __JSON__ із масивом <%= task[1] %>.

Розпарсити отриману JSON строку за допомогою спеціальних класів Qt і перевести дані у C++ колекцію елементів відповідного типу. 
Вивести отримані дані у консоль.

<%= task[2] %>. Вивести результат у консоль.

#### Матеріали:

1. [Лекція. Клієнт-серверна архітектура програм. Сокети, протоколи IP, TCP i UDP.][lec-1]

[lec-1]: https://docs.google.com/document/d/1Lm-2s3EyzOd7ynPMKzi53y3kbZ_UwagXfhWCp6dv4sw/edit?usp=sharing
